using UnityEngine;

public class MainCharacter : MonoBehaviour
{
    // Just the script to the ball to know what to touch by the targets
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
